UPSTREAM_SERVER=
JWKS_URL=$KEYCLOAK_FQDN/auth/realms/$REALM/protocol/openid-connect/certs

# log server URL and TOKEN (JWT, see https://github.com/uwcirg/logserver#access-via-jwt)
# LOGSERVER_TOKEN=
# LOGSERVER_URL=
